( function() {
	angular.module( 'blastApp' )
		.controller( 'forgotCOntroller', forgotCOntroller);

	function forgotCOntroller($scope, $location, authSrv, emailSrv ) {
		// this.to = sendEmail;

		// function sendEmail(){

		// 		$location.url('/enterEmail');

		// } 
	}
} )();